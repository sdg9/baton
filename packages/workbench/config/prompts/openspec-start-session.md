# OpenSpec Agent Session Prompt

You are working in project:

```text
{{projectPath}}
```

OpenSpec change:

```text
{{changeName}}
```

Goal:

```text
{{cardTitle}}
```

Read the project instructions first. Then inspect the OpenSpec change artifacts
under `openspec/changes/{{changeName}}/`. Summarize what you find, identify the
next concrete action, and wait for confirmation unless the user explicitly asked
for autonomous execution.

